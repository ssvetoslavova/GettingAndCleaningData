Variables

Activity_labels.txt translates the numeric activities stored in y_*.txt ranging 1 to 6, inclusive.

The subject_*.txt files in the test and train directories associate the number of the subject being recorded.

The X_*.txt files contain the actual readings. They are described by the features_info.txt - main directory of the data set.

The script combined these files and selected only the mean and standard deviation.
